# Website CLAUDE.md

## Dev Server

**DO NOT start a new dev server with `npm run dev`.** The user likely already has it running.

Instead of starting a new dev server:
1. Ask the user to check their running dev server
2. Or use `curl localhost:3000/docs/getting-started` to test if it's responding
