---
import RalphTextLogo from "../brand/RalphTextLogo.astro"
---

<div class="text-sm r4d-ralph-content">
	<div class="r4d-ralph-card__head">
		<div class="r4d-ralph-card__brand">
			<div class="r4d-ralph-card__logo-wrap">
				<img
					src="/ralphos_logo.png"
					alt="RalphOS"
					loading="lazy"
					class="r4d-ralph-card__logo"
				/>
			</div>
			<div>
				<h3 class="text-base font-medium leading-tight">
					<RalphTextLogo />
				</h3>
				<p class="text-xs font-mono text-muted-foreground mt-0.5">Agent Appliance</p>
			</div>
		</div>
	</div>

	<p class="r4d-ralph-card__description text-muted-foreground mt-3">
		RalphOS is built to host <code>ralphd</code> and isolated automation sandboxes, not to be a user-interactive desktop.
	</p>

	<div class="r4d-ralph-card__actions mt-4">
		<a href="/download/" class="r4d-ralph-card__action r4d-ralph-card__action--primary">
			Open Downloads
		</a>
		<a href="/docs/variants/" class="r4d-ralph-card__action">
			Read Variant Docs
		</a>
	</div>
</div>

<style>
	.r4d-ralph-content {
		padding: 0.15rem 0;
	}

	.r4d-ralph-card__head {
		display: flex;
		align-items: flex-start;
		gap: 0.75rem;
	}

	.r4d-ralph-card__brand {
		display: inline-flex;
		align-items: center;
		gap: 0.65rem;
		min-width: 0;
	}

	.r4d-ralph-card__logo-wrap {
		width: 2rem;
		height: 2rem;
		flex: 0 0 auto;
		display: grid;
		place-items: center;
	}

	.r4d-ralph-card__logo {
		width: 100%;
		height: 100%;
		object-fit: contain;
	}

	.r4d-ralph-card__description {
		line-height: 1.4;
	}

	.r4d-ralph-card__actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.r4d-ralph-card__action {
		display: inline-flex;
		height: 2.15rem;
		align-items: center;
		justify-content: center;
		padding: 0 0.72rem;
		font-size: 0.74rem;
		font-weight: 600;
		background: color-mix(in oklab, var(--muted) 56%, transparent);
		color: var(--foreground);
		border: 1px solid color-mix(in oklab, var(--foreground) 18%, transparent);
		transition: background-color 140ms ease, border-color 140ms ease;
	}

	.r4d-ralph-card__action:hover {
		background: color-mix(in oklab, var(--muted) 72%, transparent);
	}

	.r4d-ralph-card__action--primary {
		background: color-mix(in oklab, var(--primary) 72%, transparent);
		border-color: color-mix(in oklab, var(--primary) 65%, transparent);
		color: var(--primary-foreground);
	}

	.r4d-ralph-card__action--primary:hover {
		background: color-mix(in oklab, var(--primary) 82%, transparent);
	}
</style>
