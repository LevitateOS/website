---
interface Props {
	delay?: number
	class?: string
}

const { delay = 0, class: className = "" } = Astro.props
---

<div class:list={["catalog-reveal", className]} style={`animation-delay: ${delay}ms;`}>
	<slot />
</div>
