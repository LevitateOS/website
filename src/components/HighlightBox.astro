---
interface Props {
	label?: string
	class?: string
}

const { label = "NOTE", class: className = "" } = Astro.props
---

<div class:list={["ui-panel ui-panel-muted p-4 border-l-[8px] border-[color:var(--accent)]", className]}>
	<p class="ui-label mb-2">{label}</p>
	<slot />
</div>
